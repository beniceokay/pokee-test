#!/usr/bin/env bash
# Checkout launcher: Claude Code with pokee-isaac as an extra /model entry.
# pip/pipx installs get the `claude-pokee` command, which does the same thing.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$ROOT/pokee_cli.py" run "$@"
